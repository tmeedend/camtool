[ABOUT]
NAME = CamTool 3 POC
AUTHOR = tmeedend
VERSION = 0.1
DESCRIPTION = Capability probe for the CamTool 3 Lua/CSP port. Tests whether CSP Lua APIs can replace the calls CamTool 2 still makes into CamTool_1-16.dll. Does not touch CamTool 2.

[CORE]
; FULL: unload completely when the window is closed, so a stale grabbed camera
; can never survive a closed window during testing.
LAZY = FULL

[WINDOW_...]
ID = main
NAME = CamTool 3 POC
FUNCTION_MAIN = windowMain
MIN_SIZE = 320, 400
SIZE = 360, 620
